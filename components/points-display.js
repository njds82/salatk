// ========================================
// Points Display Component
// ========================================

// Already handled in main header, but helpers here
function formatPointsChange(amount) {
    return amount > 0 ? `+${amount}` : `${amount}`;
}
